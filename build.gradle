plugins {
    // only lock external plugin versions here—do NOT apply java/application
    id 'org.springframework.boot' version '3.1.5' apply false
    id 'io.spring.dependency-management' version '1.1.0' apply false
}

allprojects {
    repositories {
        mavenCentral()
    }
}
